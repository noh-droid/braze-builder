import { defaultConfig } from "./defaultConfig";
import { generateCode } from "./generateCode";
import Preview from "./Preview";
import Settings from "./Settings";

export default {
  id: "slide-up-modal",
  label: "Slide Up Modal",
  description: "하단 슬라이드 업 팝업 (이미지+텍스트 랜딩 버튼, 원형 X 닫기)",
  defaultConfig,
  generateCode,
  Preview,
  Settings,
};
